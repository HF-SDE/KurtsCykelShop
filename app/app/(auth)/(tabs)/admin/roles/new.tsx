import { Box } from "@components/ui/box";
import { Text } from "@components/ui/text";

export default function NewPage() {
    return (
        <Box className="flex-1 items-center justify-center">
            <Text size="2xl">Ny rolle</Text>
        </Box>
    );
}