export interface LocationLog {
  id: string;
  locationId: string;
  tableField: string;
  oldLocationId: string;
  newLocationId: string;
  changedById: string;
  changedByName: string;
  createdAt: string;
  updatedAt: string;
}
